# PostgreSQL connection string (set automatically by Vercel Neon integration)
DATABASE_URL="postgresql://user:password@host/db"

# Anthropic API key for AI-powered item extraction and catalog matching
# Without this the app falls back to local keyword matching
ANTHROPIC_API_KEY="sk-ant-..."
